import styled from "styled-components"

export const VarietiesRow = styled.div`
  display: flex;
  justify-content: space-around;
  margin-bottom: 24px;

  a { 
    text-decoration: none;
  }
`

export const IconContainer = styled.div`
  align-self: center;
  font-size: 20px;
  color: var(--grey-600);
  line-height: 1;
`